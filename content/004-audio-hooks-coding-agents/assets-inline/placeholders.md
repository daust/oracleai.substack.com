screenshot-01-hooks-verify.png — /hooks output inside a Claude Code session showing the three hooks (Notification, PreToolUse, Stop) loaded and active
screenshot-02-permission-prompt.png — a Claude Code permission prompt (the moment the attention cue fires), so readers recognize what they're listening for
